# Создание новых карточек - Документация

## Обзор функциональности

Добавлена возможность создания новых карточек через пользовательский интерфейс. Пользователи могут нажать на кнопку "+" рядом со счетчиком результатов, чтобы открыть модальное окно для создания новой карточки с пустыми полями вопроса и ответа.

## Измененные файлы

### 1. `script.js`
**Что изменено:**
- Добавлена кнопка создания карточки (`#create-card-btn`) рядом со счетчиком результатов
- Добавлен обработчик клика для открытия модального окна создания
- Обновлена структура `#results-count-container` для поддержки flex-layout

**Строки:** 742-770

### 2. `ui-variants/tabs-navigation.js`
**Что изменено:**
- Добавлена кнопка создания карточки в расширенную навигацию с табами
- Добавлен обработчик клика для кнопки создания
- Обновлен HTML-шаблон счетчика результатов

**Строки:** 3846-3875

### 3. `srs/learn-ui.js`
**Что изменено:**
- Добавлен импорт `buildCategoriesFromData` из `computed-categories.js`
- Добавлена helper-функция `getRuntimeData()` для получения данных карточек
- Добавлено состояние `createModalState` для отслеживания состояния модального окна создания
- Добавлена функция `openCreateModal()` - открывает модальное окно с:
  - Выпадающими списками категорий/подкатегорий (вверху, 2 колонки)
  - Панелью форматирования (во всю ширину)
  - Полем вопроса (contenteditable div)
  - Полем ответа (contenteditable div)
  - Кнопками Отмена/Создать
  - **Порядок и стили КАК В РЕДАКТИРОВАНИИ** на главной странице
- Добавлена функция `closeCreateModal()` - закрывает модальное окно создания
- Добавлена функция `saveCreateChanges()` - сохраняет новую карточку на сервере и в localStorage (с категорией и подкатегорией)
- Добавлена функция `showCreateNotification()` - показывает уведомления о создании карточки
- Добавлены inline-стили и адаптивные стили для полного соответствия окну редактирования
- Функции экспортированы в глобальную область через `window.openCreateModal`, `window.saveCreateChanges`, `window.closeCreateModal`

**Строки:** 10, 14-23, 919-1160, 1217-1386, 3280-3284

### 4. `server.js`
**Что изменено:**
- Добавлен новый endpoint `POST /api/card/create` для создания карточек
- Валидация обязательных полей (question, answer, username)
- Создание карточки с переданными категорией и подкатегорией (или значения по умолчанию)
- Сохранение formatting данных
- Обновление метаданных файла
- Логирование операции создания

**Строки:** 1488-1597

### 5. `custom-styles.css`
**Что изменено:**
- Добавлены стили для уведомлений создания (`.create-notification`, `.create-notification.success`, `.create-notification.error`, и т.д.)
- Добавлены мобильные стили для уведомлений создания
- Добавлен кастомный минималистичный скроллбар для модальных окон `.edit-modal`:
  - Ширина 8px
  - Полупрозрачный трек (rgba черный)
  - Полупрозрачный ползунок (rgba белый)
  - Эффект hover для ползунка
  - Поддержка Firefox через scrollbar-width/scrollbar-color
- **ИСПРАВЛЕНИЕ ПЕРЕПОЛНЕНИЯ ТЕКСТА**: Добавлены критические CSS правила для переноса текста в карточках:
  - `word-wrap: break-word !important` для всех текстовых элементов
  - `overflow-wrap: break-word !important` для всех текстовых элементов
  - `max-width: 100% !important` для всех дочерних элементов flashcard
  - `word-break: break-word !important` для контейнеров контента
  - `hyphens: auto !important` для автоматической расстановки переносов

**Строки:** 3049-3099, 3128-3132, 3099-3124, 3227-3264

### 6. `tests/api/card-creation.test.js` (НОВЫЙ ФАЙЛ)
**Что добавлено:**
- Полный набор интеграционных тестов для API создания карточек
- Тесты успешного создания
- Тесты валидации (отсутствие вопроса, ответа, username)
- Тесты создания карточки с форматированием

## Как использовать

### Через пользовательский интерфейс:
1. Откройте главную страницу приложения
2. Найдите счетчик результатов (например, "Найдено: 5" или "Всего карточек: 42")
3. Нажмите на кнопку "+" (плюс) рядом со счетчиком
4. Откроется модальное окно **точно такое же как при редактировании карточки**, с:
   - Выпадающими списками категории и подкатегории (вверху, 2 колонки)
   - Панелью форматирования текста
   - Пустыми полями вопроса и ответа (contenteditable divs)
5. Выберите категорию и подкатегорию из выпадающих списков
6. Заполните вопрос и ответ (оба поля обязательны)
7. При необходимости примените форматирование текста
8. Нажмите "Создать" или используйте `Ctrl+Enter` для сохранения
9. Нажмите "Отмена" или `Escape` для отмены

**Порядок блоков в модальном окне:**
1. Категория | Подкатегория (в 2 колонки)
2. Панель форматирования
3. Вопрос
4. Ответ
5. [Отмена] [Создать] (кнопки внизу справа)

### Через API:
```javascript
POST /api/card/create?username={username}
Content-Type: application/json

{
  "question": "Текст вопроса",
  "answer": "Текст ответа",
  "category": "Категория (необязательно, по умолчанию: 'Без категории')",
  "subcategory": "Подкатегория (необязательно, по умолчанию: 'Общее')",
  "formatting": {
    "question": [],
    "answer": []
  }
}
```

**Ответ:**
```json
{
  "ok": true,
  "card": {
    "question": "Текст вопроса",
    "answer": "Текст ответа",
    "formatting": {...}
  },
  "totalCards": 43
}
```

## Технические детали

### Выпадающие списки категорий и подкатегорий

Модальное окно создания включает два связанных выпадающих списка:
- **Категория** - заполняется динамически из всех доступных категорий в системе
- **Подкатегория** - автоматически обновляется при изменении категории

Связь между списками работает следующим образом:
1. При открытии модального окна загружаются все категории из `buildCategoriesFromData(getRuntimeData())`
2. При выборе категории список подкатегорий обновляется соответствующими подкатегориями
3. Если у категории нет подкатегорий, используется значение по умолчанию "Общее"

### Поток данных:
1. Пользователь нажимает кнопку "+" → вызывается `openCreateModal()`
2. Открывается модальное окно с пустыми `contenteditable` редакторами И выпадающими списками категорий/подкатегорий
3. Пользователь выбирает категорию и подкатегорию из выпадающих списков (динамически связанных)
4. Пользователь заполняет вопрос и ответ и нажимает "Создать"
5. `saveCreateChanges()` извлекает текст, форматирование, категорию и подкатегорию из модального окна
6. Отправляется POST запрос на `/api/card/create` с категорией и подкатегорией
7. Сервер создает новую карточку с указанными категорией и подкатегорией
8. Карточка сохраняется в файл `data/user_{username}.json`
9. localStorage обновляется новой карточкой
10. Генерируется событие `qaDataUpdated` для обновления UI
11. Показывается уведомление об успехе

### Структура данных карточки:
```javascript
{
  category: "Выбранная категория",
  subcategory: "Выбранная подкатегория",
  question: "Текст вопроса",
  answer: "Текст ответа",
  formatting: {
    question: [{ start, end, color, bold, italic, underline, code }],
    answer: [...]
  }
}
```

### Горячие клавиши в модальном окне:
- `Ctrl+Enter` - Создать карточку
- `Escape` - Отменить создание

### Валидация:
- Вопрос не может быть пустым
- Ответ не может быть пустым
- Пользователь должен быть авторизован

## Тестирование

### Запуск тестов:
```bash
npm run test:api
```

Или только тесты создания:
```bash
npm run test:setup && node --experimental-vm-modules node_modules/jest/bin/jest.js tests/api/card-creation.test.js
```

### Покраска тестов:
- ✅ Успешное создание карточки
- ✅ Валидация обязательных полей
- ✅ Создание карточки с форматированием
- ✅ Проверка сохранения в файл

## Совместимость

- Работает в обоих режимах отображения (обычный поиск и навигация с табами)
- Поддержка мобильных устройств (адаптивные стили)
- Совместимо с существующей системой аутентификации
- Использует те же стили модальных окон, что и редактирование

## Будущие улучшения

Возможные улучшения в будущем:
1. Возможность выбора категории/подкатегории при создании
2. Шаблоны карточек для быстрого создания
3. Массовое создание карточек (импорт)
4. Предпросмотр карточки перед созданием
5. Отмена создания с сохранением черновика

## Известные ограничения

- ~~Новая карточка создается с категориями по умолчанию ("Без категории", "Общее")~~ ✅ ИСПРАВЛЕНО: Теперь можно выбрать категорию и подкатегорию
- ~~Текст новых карточек выпадает за границы контейнера~~ ✅ ИСПРАВЛЕНО: Добавлены CSS правила для переноса текста
- Для изменения категории после создания необходимо отредактировать карточку
- Форматирование применяется, но по умолчанию пустое
